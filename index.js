// This is just a test publis function

export function transfer(accountName, amount, accountNo){
    return `Transfered ${amount} to account number ${accountNo} registered with the name ${accountName}`
}